<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeginnersToAdvanced"
        tools:targetApi="31">
        <activity
            android:name=".viewpager2.ViewPager2Activity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".fragments.FragmentActivity"
            android:exported="false"/>

        <activity
            android:name=".alertdialog.AlertDialogActivity"
            android:exported="false" />
        <activity
            android:name=".spinners.SpinnerActivity"
            android:exported="false" />
        <activity
            android:name=".recyclerviews.RecyclerViewActivity"
            android:exported="false" />
        <activity
            android:name=".listviews.ListViewActivity"
            android:exported="false" />
        <activity
            android:name=".intents.SecondIntentActivity"
            android:exported="false" />
        <activity
            android:name=".intents.FirstIntentActivity"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>